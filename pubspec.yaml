name: budget_manager_application
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.2.3

dependencies:
  flutter:
    sdk: flutter

  fl_chart: ^0.66.0
  english_words: ^4.0.0
  provider: ^6.1.1
  intl: ^0.19.0
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3 
  logger: ^2.0.2+1
  flutter_spinkit: ^5.2.0 
  cloud_firestore: ^4.13.6 
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true